def source_policy_text() -> str:
    return "\n".join(("News source policy", "Mock/local sources by default.", "Phase 24 validates any explicit public URL; private, authenticated, session, and credential targets are blocked."))
